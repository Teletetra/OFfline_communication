import { Module } from '@nestjs/common'; import { BullModule } from '@nestjs/bull'; import { QueueService } from './queue.service'; @Module({imports:[BullModule.forRoot({redis:{host:process.env.REDIS_HOST||'localhost',port:Number(process.env.REDIS_PORT||6379)}}),BullModule.registerQueue({name:'messages'},{name:'notifications'},{name:'cleanup'})],providers:[QueueService],exports:[QueueService]}) export class QueueModule {}