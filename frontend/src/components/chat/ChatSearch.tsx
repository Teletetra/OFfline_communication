import React,{ChangeEvent}from'react';import{useChatStore}from'../../store/chatStore';export default function ChatSearch(){const [q,setQ]=React.useState('');const conversations=useChatStore(s=>s.conversations);const filtered=conversations.filter(c=>JSON.stringify(c).toLowerCase().includes(q.toLowerCase()));return <div className="chat-search"><input value={q} onChange={(e:ChangeEvent<HTMLInputElement>)=>setQ(e.target.value)} placeholder="Search chats…"/>{q&&<div>{filtered.length} conversation(s) found</div>}</div>}
