import React,{FormEvent,useState}from'react';import{authService}from'../../services/auth.service';export default function ForgotPasswordForm(){const[email,setEmail]=useState('');const[status,setStatus]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();try{await authService.forgotPassword(email);setStatus('Reset instructions requested.');}catch(err){setStatus(err instanceof Error?err.message:'Request failed');}};return <form className="auth-form" onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button>Send reset instructions</button>{status&&<p>{status}</p>}</form>}
