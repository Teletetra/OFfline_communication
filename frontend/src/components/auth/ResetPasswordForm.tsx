import React,{FormEvent,useState}from'react';import{authService}from'../../services/auth.service';export default function ResetPasswordForm({token}:{token:string}){const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[status,setStatus]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();if(p1!==p2){setStatus('Passwords do not match');return;}try{await authService.resetPassword(token,p1);setStatus('Password reset successfully.');}catch(err){setStatus(err instanceof Error?err.message:'Reset failed');}};return <form className="auth-form" onSubmit={submit}><input type="password" minLength={8} required value={p1} onChange={e=>setP1(e.target.value)} placeholder="New password"/><input type="password" minLength={8} required value={p2} onChange={e=>setP2(e.target.value)} placeholder="Confirm password"/><button>Reset password</button><p>{status}</p></form>}
