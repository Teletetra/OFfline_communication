import React,{FormEvent,useState}from'react';import{useAuthStore}from'../store/authStore';import'./Profile.css';
export default function Profile(){const{user,updateUser}=useAuthStore();const [username,setUsername]=useState(user?.username||'');const [bio,setBio]=useState((user as any)?.bio||'');const save=(e:FormEvent)=>{e.preventDefault();updateUser({username,...({bio} as any)});};return <main className="profile-page"><h1>Profile</h1><form onSubmit={save}><label>Username<input value={username} onChange={e=>setUsername(e.target.value)}/></label><label>Bio<textarea value={bio} onChange={e=>setBio(e.target.value)} maxLength={500}/></label><label>Email<input value={user?.email||''} disabled/></label><button type="submit">Save profile</button></form></main>}
