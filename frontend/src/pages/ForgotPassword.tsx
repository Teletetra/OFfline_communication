import React,{FormEvent,useState}from'react';import{Link}from'react-router-dom';import{authService}from'../services/auth.service';export default function ForgotPassword(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{await authService.forgotPassword(email);setMessage('If the account exists, reset instructions have been requested.');}catch(err){setError(err instanceof Error?err.message:'Request failed');}};return <main className="simple-page"><h1>Forgot password</h1><form onSubmit={submit}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><button>Send reset link</button></form>{message&&<p>{message}</p>}{error&&<p>{error}</p>}<Link to="/login">Back to login</Link></main>}
