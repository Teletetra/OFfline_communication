import React from 'react';export default function Privacy(){return <main className="simple-page"><h1>Privacy</h1><p>Messages are intended to be encrypted in transit and locally protected. Avoid storing production secrets in browser storage beyond what your threat model permits.</p><p>Offline data should be treated as sensitive because client-side storage can be accessed by the local user profile.</p></main>}
