import React,{FormEvent,useState}from'react';import{useSearchParams,Link,useNavigate}from'react-router-dom';import{authService}from'../services/auth.service';export default function ResetPassword(){const[params]=useSearchParams();const token=params.get('token')||'';const nav=useNavigate();const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();if(p1!==p2){setError('Passwords do not match');return;}try{await authService.resetPassword(token,p1);nav('/login',{replace:true});}catch(err){setError(err instanceof Error?err.message:'Reset failed');}};return <main className="simple-page"><h1>Reset password</h1><form onSubmit={submit}><input type="password" minLength={8} required value={p1} onChange={e=>setP1(e.target.value)} placeholder="New password"/><input type="password" minLength={8} required value={p2} onChange={e=>setP2(e.target.value)} placeholder="Confirm password"/><button>Reset password</button></form>{error&&<p>{error}</p>}<Link to="/login">Back to login</Link></main>}
